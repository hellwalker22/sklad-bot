meta:
  environment: python
  toolchain:
    name: pip
    version: 3.11.3
build:
  requirementsPath: sklad-bot/requirements.txt
run:
  scriptName: sklad-bot/bot.py
  persistenceMount: /data
  containerPort: 80
